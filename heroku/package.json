{
  "name": "poulin-webhook-service",
  "version": "1.0.0",
  "description": "Servizio webhook per Poulin con architettura DDD",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "start": "node dist/server.js",
    "build": "tsc",
    "dev": "nodemon --exec ts-node src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "tsc"
  },
  "dependencies": {
    "@types/bcrypt": "*",
    "@types/cors": "*",
    "@types/express": "*",
    "@types/jsonwebtoken": "*",
    "@types/node": "*",
    "@types/pg": "*",
    "bcrypt": "*",
    "cors": "*",
    "dotenv": "*",
    "express": "*",
    "express-rate-limit": "*",
    "helmet": "*",
    "jsonwebtoken": "*",
    "nodemon": "*",
    "pg": "*",
    "ts-node": "*",
    "typescript": "*"
  },
  "devDependencies": {
    "@types/node": "^20.17.24",
    "@types/pg": "^8.11.11",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1"
  },
  "engines": {
    "node": "18.18.0"
  }
}
